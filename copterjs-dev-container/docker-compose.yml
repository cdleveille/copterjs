version: "3.7"
services:
    copterjs-dev-container:
        container_name: copterjs-dev-container
        image: postgres
        restart: unless-stopped
        volumes:
            - ./data/db:/var/lib/postgresql/data
        env_file:
            - database.env
        ports:
            - "6668:5432"
        networks:
            - copterjs-dev-network
networks:
    copterjs-dev-network:
        driver: bridge
# Run 'docker network create copterjs-dev-network' before composing up
