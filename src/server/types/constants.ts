/* eslint-disable no-unused-vars */

export enum Host {
	local = "localhost",
	ip = "127.0.0.1"
}

export enum Env {
	dev = "development",
	prod = "production"
}

export enum Routes {
	root = "/",
	top10 = "/top10"
}
