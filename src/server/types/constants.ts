/* eslint-disable no-unused-vars */

export enum Host {
	dev = "localhost",
	prod = "copterjs.herokuapp.com"
}

export enum Env {
	dev = "development",
	prod = "production"
}

export enum Routes {
	root = "/",
	top10 = "/top10"
}
