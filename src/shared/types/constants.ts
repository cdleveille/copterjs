/* eslint-disable no-unused-vars */

export enum Env {
	dev = "development",
	prod = "production"
}

export enum Routes {
	root = "/",
	top10 = "/top10",
	newScore = "/newscore",
	websocketClient = "/websocket-client"
}
